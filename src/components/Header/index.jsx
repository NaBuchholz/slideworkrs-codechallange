import { NavBar } from '..';
import { ReactComponent as Logo } from '../../assets/logo.svg';
const Header = () => {
	return (
		<div>
			<Logo />
			<NavBar />
		</div>
	);
};

export default Header;
